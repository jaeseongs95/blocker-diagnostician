{
  "name": "blocker-diagnostician",
  "version": "1.0.0",
  "description": "Separate repeated failures into causal hypotheses and select the next discriminating test.",
  "type": "module",
  "bin": {
    "blocker-diagnostician": "./scripts/cli.mjs"
  },
  "scripts": {
    "cli": "node scripts/cli.mjs",
    "cluster": "node scripts/cluster-failures.mjs",
    "digest": "node scripts/digest-request.mjs",
    "lint": "node scripts/check-repository.mjs",
    "test": "node --test",
    "validate": "pnpm lint && pnpm test"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.19.0",
  "license": "MIT",
  "dependencies": {
    "ajv": "8.17.1"
  }
}
